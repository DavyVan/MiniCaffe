language: cpp
os:
  - linux
dist:
  - xenial
branches:
  only:
    - master
    - fq-dev
    - yz-branch
    - mc-dev
script:
  - apt-get install -y cuda
  - cd minicaffe/build
  - cmake .. -DTEST_ENABLED=ON
  - cmake --build .